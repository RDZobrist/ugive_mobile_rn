import React from 'react';
import { Text, StyleSheet } from 'react-native';

const HistoryScreen = () => {
  return <Text style={styles.text}>History</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default HistoryScreen;

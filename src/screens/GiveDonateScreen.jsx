import React from 'react';
import { Text, StyleSheet } from 'react-native';

const GiveDonateScreen = () => {
  return <Text style={styles.text}>Give/Donate</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default GiveDonateScreen;

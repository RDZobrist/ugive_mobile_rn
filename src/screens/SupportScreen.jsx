import React from 'react';
import { Text, StyleSheet } from 'react-native';

const SupportScreen = () => {
  return <Text style={styles.text}>Support</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default SupportScreen;
